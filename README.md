# Note-taker

First project, solo and free ai agent (vs)coded.
CLI tool to transcribe audio files into text using OpenAI Whisper.

## Installation
```bash
python -m venv venv
source venv/bin/activate
pip install -e .
